import { Component } from '@angular/core';
import { Shell } from '../shell/shell';

@Component({
  selector: 'app-quiz',
  imports: [Shell],
  templateUrl: './quiz.html',
  styleUrl: './quiz.scss'
})
export class Quiz {

}
