import { Component } from '@angular/core';
import { Shell } from '../shell/shell';

@Component({
  selector: 'app-rules',
  imports: [Shell],
  templateUrl: './rules.html',
  styleUrl: './rules.scss'
})
export class Rules {

}
